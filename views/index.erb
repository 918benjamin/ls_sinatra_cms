<!doctype html>
<html lang="en-US">
  <head>
    <title>Home</title>
    <meta charset="UTF-8">
  </head>
  <body>
    <ul>
      <% @files.each do |file|%>
        <li><a href="/<%= file %>"><%= file %></a></li>
      <% end %>
    </ul>
  </body>
</html>